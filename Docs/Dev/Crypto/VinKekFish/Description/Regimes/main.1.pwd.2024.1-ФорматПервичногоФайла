Формат файла

Функция AddStartPart
---------------------------
--- Секции "Regime ".+ ---
*** 2 байта. Секция "Regime len". Имя в этом файле: asciiRegimeName.Length. Младший байт по младшему адресу.
Указывает на длину строки asciiRegimeName, которая описывает режим. Данные байты обфусцированы массивом lenObfs.

*** 2 байта. Секция "Regime len". Имя в этом файле: params.Length. Младший байт по младшему адресу.
Указывает на длину массива параметров шифрования. Данные обфусцированы массивом lenObfs.

*** 8 байтов. Секция "Regime len obfs". Имя в этом файле: lenObfs.
Массив обфусцирующей информайции. Первые 4-ре байта налагаются сложением на секцию "Regime len". Затем вторые 4-ре налагаются аналогичным образом снова.
Двойное наложение нужно, чтобы убрать видиые невооружённым глазом зависимости.
Обфускация немного усложняет распознавание формата файла неспециализированными средствами или вручную.

*** Секция "Regime obfs". Имя в этом файле: obfRegimeName.
Используется для двойной обфускации секции "Regime name". По длине равна двойной длине секции "Regime name".


-----------------------
--- Секция "Regime" ---
Вся секция обфусцирована дважды массивом obfRegimeName. Обфускация аналогична массиву lenObfs.

*** asciiRegimeName.
Обфусцированная строка в формате ASCII, которая описывает имя режима шифрования.

*** 1 байт. vkfOpt.K.
Коэффициент стойкости шифрования VinKekFish ([1,19], нечётный).

*** Variable ulong. OIV.Length.
Длина вектора OIV.

*** Variable ulong. SIB64 = cscOpt.StrengthInBytes/64.
Стойкость шифрования каскадной губки в блоках по 64-ре байта.
*** Variable ulong. ArmoringSteps.
Настройка дополнительного количества раундов для каскадной губки.
*** Variable ulong. InitSteps.
Количество раз, которое будет проинициализирована губка в InitThreeFishByCascade.
*** Variable ulong. StepsForTable.
Количество шагов губки при генерации таблицы подстановок. Параметр countOfStepsForSubstitutionTable в InitThreeFishByCascade.


*** Variable ulong. vkfRounds.
Количество раундов для VinKekFish.
*** Variable ulong. vkfPreRounds.
Количество раундов для VinKekFish с константными таблицами.

--------------------
--- Секция "OIV" ---
*** OIV.
Открытый вектор инициализации.

Класс Main_1_PWD_2024_1.CryptDataClass.
-------------------
--- "Encrypted" ---
Шифрованная часть файла.




