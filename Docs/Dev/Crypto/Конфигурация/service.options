Path
    random at start folder
        Здесь содержится путь к папке, где располагаются файлы с энтропией, которые передаются между запусками программы


output
    random
        unix stream
            path
                Здесь располагается путь к папке, в которой будут содержаться файлы криптостойкого выхода

Input
    entropy
        # Здесь содержатся разделы, описывающие источники энтропии для программы
        OS
            # Это полный путь к файлу энтропии, предоставляемому системой. Есть на всех unix-подобных системах
            /dev/random
                # Внутри раздела содержится информация о качестве предоставляемой энтропии
                min
                    480
                    # Это минимальная предоставляемая энтропия. Используется для оценки содержащейся энтропии
                    # Означает, что на 480 битов, считанных из этого источника, приходится один настоящий бит энтропии
                    # Это значение можно менять как хочется, однако это повлияет на оценку энтропии
                    # Слишком большое значение даст задержку при получении энтропии из выхода программы
                    # Слишком малое значение может повлечь за собой недостаток энтропии в выходе программы, что может повлечь за собой полную расшифровку информации
                max
                    1
                    # Это значение используется для оценки верхней границы энтропии. Чем выше это значение, тем реже данные будут поступать в основную губку для накопления.
                EME
                    2048
                    # Значение, аналогичное min, но для оценки энтропии, которая является стойкой к перехвату по побочным каналам
                interval
                    # Интервал, с которым нужно собирать данные из указанного источника
                    once
                        # once - сбор один раз при старте программы
                        length
                            32
                            # Собирается 32 байта
                        date
                            yes
                            # Дополнительно к источнику собирается точное время, в которое была собрана энтропия из этого источника
                    1s
                        # Дополнительно к однократному сбору - сбор каждую секунду
                        length
                            1
                            # Собирается 1 байт
                        date
                            no
                            # Время сбора не добавляется
                        difference
                            # Энтропия собирается, даже если данные из источника не отличаются раз от раза
                            no

