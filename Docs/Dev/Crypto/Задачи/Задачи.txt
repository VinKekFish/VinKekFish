CascadeSponge_20230905_BaseTest_noonce

При инициализации, похоже, зависает на выполнении
ps -eo pid,times,rss,nlwp,args --sort time,rss,pid,args
Поменять waitforexit на ожидание с таймаутом
/Arcs/Repos/Crypto/VinKekFish/src/main/5 main-crypto/exe/service/entropy/ExecEntropy-init.cs:319



Проверить, что при генерации файлов учтены все параметры настроек. Удалить лишние параметры настроек и пройтись по генерации.
Разобраться, какие настройки, в итоге, будут использованы при генерации ключа, а какие при шифровании файла непосредственно.
Проверить, что длина внешней синхропосылки не меньше, чем длина ключа

Сделать главные тесты.
Сделать для Record тесты на | (concat). Сейчас операция нигде не используется.

Не забыть проверить потоки данных (вручную), особенно при генерации энтропии. А ещё лучше, и тесты сделать, и в отладчике пройти.

Выполнить задачи из списка TODO

Проверить модель угроз на то, что всё выполнено. В том числе, нужно лучше реализовать очистку памяти на случай сбоев (возможно, сразу на уровне общего аллокатора и ручного завершения программы).
Record.doExceptionOnDisposeInDestructor = false; и ещё один флаг установить в true в vkf.

Попробовать начертить схему криптографических преобразований и, главное, преобразования пароля
Схема VinKekFish

Сделать автотесты на keccak, на таблицы перестановок (чтобы они не изменялись, если что-то будет изменено)

Проверить, что там в /Arcs/Repos/Crypto/VinKekFish/Docs/Dev/Crypto/VinKekFish/Description/ФилософскиеРазмышления/ и в записках в этой директории

Проверить, что не допущено ошибок, описанных в https://github.com/VinCryptoOS/vincryptoos.github.io


Закончить всю документацию
    В документации написать по поводу того, что сообщения типа "Fatal Error" не устранимы пользователем и являются ошибками программирования. Если они возникли, то нужно обратиться к разработчику.
    Написать по поводу использования cat для ряда специальных файлов типа /proc/cpuinfo и т.п.
    Написать по поводу того, что файлы настроек должны быть под рутом (а может, сделать проверку?)

Сделать отдельную команду для проверки файлов-настроек для сервиса
wait and once
Сделать анализ различий при сборе энтропии


Необходимо сделать памятку пользователю, что делать, если программа не запускается, потому что не выполняются cmd
Сделать нормальный вывод данных, если cmd не запускается. Чтобы было понятно, о чём речь.


Всё-таки, попробовать сделать автоматические тесты на инициализацию губки.

fuse делает, при выдаче энтропии, копию. Эта копия не очищается.

Шифрование файла подкачки
    https://qaa-engineer.ru/kak-zashifrovat-swap-v-linux/
    https://askubuntu.com/questions/248158/how-do-i-setup-an-encrypted-swap-file
        cryptsetup
        https://web.archive.org/web/20160504140513/http://hydra.geht.net/tino/howto/linux/cryptswap/
    https://www.geeksforgeeks.org/how-to-setup-encrypted-filesystems-and-swap-space-using-cryptsetup-tool-in-linux/

